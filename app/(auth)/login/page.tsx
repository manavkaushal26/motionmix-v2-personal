import LoginForm from "@/components/forms/login";

type Props = {};

const Page = (props: Props) => {
  return (
    <div className="w-full h-screen">
      <LoginForm />
    </div>
  );
};

export default Page;
